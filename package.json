{
  "name": "pokedex-app",
  "version": "1.0.0",
  "description": "react native application for course certification",
  "main": "index.js",
  "scripts": {
    "app": "cd app && npm run start",
    "auth": "cd auth-api && npm run start:dev",
    "dev": "npm-run-parallel auth app ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-run-parallel": "^0.6.0"
  }
}
